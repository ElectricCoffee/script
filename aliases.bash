#!/bin/bash

alias sudo="sudo "

alias install="apt-get install -y"

alias uninstall="apt-get purge"

alias gstat="git status"

alias fuckit='git commit -am "$(curl http://whatthecommit.com/index.txt)"'
